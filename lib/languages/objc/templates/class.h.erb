
<%= autogenerated_warning %>

#import "TalkObjectList.h"

<%= comment_block(cls) %>

@interface <%= "#{truncated_name(cls)}" %> : <%= "#{superclass(cls)}" %>
{
    <% cls[:field].each do |field| %>
        <%= comment_block(field) %>
        <%= field_definition(cls, field) %>; // <%= field[:type].reverse.join('') %>
    <% end %>
}

<% cls[:field].each do |field| %>
@property (readwrite,strong,nonatomic) <%= field_definition(cls, field) %>;
<% end %>

+(NSString *) fullyQualifiedClassName;

@end
